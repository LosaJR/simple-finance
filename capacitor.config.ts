import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.losajr.simplefinance',
  appName: 'Simple Finance',
  webDir: 'dist'
};

export default config;
